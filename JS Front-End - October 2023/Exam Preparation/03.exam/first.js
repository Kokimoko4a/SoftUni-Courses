function solve(input){

   let countLines = input.shift();
   let players = {};

   for (let index = 0; index < input.length; index++) {
    
    let command = input[index].split(":").shift();

        if (command ===) {
            
        }

        else if (command === '') {
            
        }

        else if (condition) {
            
        }

        else {

            let name = command;
            let taskId = input[index].split(":").shift();
            let title = input[index].split(":").shift();
            let status = input[index].split(":").shift();
            let estimatedPoints = parseInt(input[index].split(":").shift());

            let asignee = name: {

                assgnee

            }

        }
    
   }


}

solve([
    '5',
    'Kiril:BOP-1209:Fix Minor Bug:ToDo:3',
    'Mariya:BOP-1210:Fix Major Bug:In Progress:3',
    'Peter:BOP-1211:POC:Code Review:5',
    'Georgi:BOP-1212:Investigation Task:Done:2',
    'Mariya:BOP-1213:New Account Page:In Progress:13',
    'Add New:Kiril:BOP-1217:Add Info Page:In Progress:5',
    'Change Status:Peter:BOP-1290:ToDo',
    'Remove Task:Mariya:1',
    'Remove Task:Joro:1']);